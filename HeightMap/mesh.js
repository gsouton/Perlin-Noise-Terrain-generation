function terrainMesh(heightmap){
    let width = heightmap.width
    let height = heightmap.height;

    for(let y = 0; y < height; y++){
        for(let x = 0; x < width; x++){
            
        }
    }
}